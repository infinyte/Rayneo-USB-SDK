<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <UseWPF>true</UseWPF>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <ApplicationManifest>app.manifest</ApplicationManifest>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="System.Speech" Version="10.0.10" />
    <!-- Local Whisper recognizer (second ISpeechToText engine). NAudio captures
         16 kHz/16-bit/mono; Whisper.net runs whisper.cpp in-process; the Runtime
         package ships the win-x64 CPU native binaries. -->
    <PackageReference Include="NAudio" Version="2.3.0" />
    <PackageReference Include="Whisper.net" Version="1.9.1" />
    <PackageReference Include="Whisper.net.Runtime" Version="1.9.1" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\RayNeo.Device\RayNeo.Device.csproj" />
    <ProjectReference Include="..\RayNeo.Voice\RayNeo.Voice.csproj" />
  </ItemGroup>

  <!-- Bundled HUD themes: copied next to the executable so the theme CLI flag
       resolves from the app's Themes folder out of the box. Drop-in themes the
       user adds beside the exe (or points at with an absolute path / the
       RAYNEO_HUD_THEME variable) are discovered the same way at runtime. -->
  <ItemGroup>
    <None Update="Themes\**\*">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

</Project>
